package com.example.kreedaprerana.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Blue ──
val Blue600 = Color(0xFF2563EB)
val Blue700 = Color(0xFF1D4ED8)
val Blue800 = Color(0xFF1E40AF)
val Blue100 = Color(0xFFDBEAFE)
val Blue50 = Color(0xFFEFF6FF)

// ── Orange Accent ──
val Orange500 = Color(0xFFF97316)
val Orange400 = Color(0xFFFB923C)
val Orange100 = Color(0xFFFFEDD5)
val Orange50 = Color(0xFFFFF7ED)

// ── Neutrals (darkened for readability) ──
val Slate900 = Color(0xFF0F172A)   // Primary text — near-black
val Slate700 = Color(0xFF2D3A4A)   // Secondary text — darkened from #334155
val Slate500 = Color(0xFF475569)   // Muted text — darkened from #64748B
val Slate400 = Color(0xFF6B7B8F)   // Hint/caption — darkened from #94A3B8
val Slate300 = Color(0xFFCBD5E1)   // Borders
val Slate200 = Color(0xFFE2E8F0)   // Dividers
val Slate100 = Color(0xFFF1F5F9)   // Light background
val Slate50 = Color(0xFFF8FAFC)    // Lightest background

// ── Semantic ──
val Success = Color(0xFF059669)    // Darkened green for readability
val SuccessLight = Color(0xFFD1FAE5)
val Warning = Color(0xFFD97706)    // Darkened amber for readability
val WarningLight = Color(0xFFFEF9C3)
val Error = Color(0xFFDC2626)      // Slightly darker red
val ErrorLight = Color(0xFFFEE2E2)

// ── Surface ──
val CardWhite = Color(0xFFFFFFFF)
val Background = Color(0xFFF8FAFC)
val DividerColor = Color(0xFFE2E8F0)

// ── Gold / Silver / Bronze for Leaderboard ──
val Gold = Color(0xFFD4A017)       // Darker gold for readability
val GoldLight = Color(0xFFFFF8E1)
val Silver = Color(0xFF8E8E8E)     // Darker silver
val SilverLight = Color(0xFFF5F5F5)
val Bronze = Color(0xFFCD7F32)
val BronzeLight = Color(0xFFFFF3E0)